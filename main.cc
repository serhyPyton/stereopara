#include <iostream>
#include "reader.h"

int max_d = 50;

int main(){
    img.init("aloe/view0.png");

    auto row1 = img.row(0);
    auto cache = std::vector<std::vector<double>(max_d)>(img.width);

    

    return 0;
}